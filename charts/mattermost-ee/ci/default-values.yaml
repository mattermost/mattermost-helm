mattermostApp:
  replicaCount: 1
  persistence:
    pluginsServer:
        enabled: false
    pluginsClient:
        enabled: false
    configApp:
        enabled: false

mysqlha:
  mysqlha:
    replicaCount: 1
  persistence:
    storageClass: "hostpath"
    accessMode: ReadWriteOnce
    enabled: true
    size: 100Mi

minio:
  persistence:
    enabled: true
    storageClass: "hostpath"
    accessMode: ReadWriteOnce
    size: 100Mi
