Mattermost has been deployed!

IMPORTANT: This is a minimal deployment requiring external database and file storage.
Make sure you have configured:
  - External database connection (PostgreSQL)
  - File storage via environment variables (S3, GCS, etc.)

Get the application URL by running these commands:
{{- if .Values.mattermostApp.ingress.enabled }}
  Ingress is enabled. Access Mattermost at:
  {{- range $host := .Values.mattermostApp.ingress.hosts }}
    http{{ if $.Values.mattermostApp.ingress.tls }}s{{ end }}://{{ $host }}
  {{- end}}
{{- else if contains "NodePort" .Values.mattermostApp.service.type }}
  export NODE_PORT=$(kubectl get --namespace {{ .Release.Namespace }} -o jsonpath="{.spec.ports[0].nodePort}" services {{ include "mattermost.fullname" . }})
  export NODE_IP=$(kubectl get nodes --namespace {{ .Release.Namespace }} -o jsonpath="{.items[0].status.addresses[0].address}")
  echo http://$NODE_IP:$NODE_PORT
{{- else if contains "LoadBalancer" .Values.mattermostApp.service.type }}
  NOTE: It may take a few minutes for the LoadBalancer IP to be available.
  export SERVICE_IP=$(kubectl get svc --namespace {{ .Release.Namespace }} {{ include "mattermost.fullname" . }} -o jsonpath='{.status.loadBalancer.ingress[0].ip}')
  echo http://$SERVICE_IP:{{ .Values.mattermostApp.service.externalPort }}
{{- else if contains "ClusterIP"  .Values.mattermostApp.service.type }}
  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "mattermost.name" . }}" -o jsonpath="{.items[0].metadata.name}")
  echo "Visit http://127.0.0.1:8065 to use your application"
  kubectl port-forward $POD_NAME 8065:{{ .Values.mattermostApp.service.externalPort }}
{{- end }}

To check the status of your deployment:
  kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name={{ include "mattermost.name" . }}"
