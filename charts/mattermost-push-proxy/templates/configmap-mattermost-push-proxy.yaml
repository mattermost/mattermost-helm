apiVersion: v1
kind: ConfigMap
metadata:
  name: {{template "fullname" .}}-push-config-template
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    app: {{ template "fullname" . }}
data:
  push-config.tpl: |
{{include "push-config.tpl" . | printf "%s" | indent 4}}
